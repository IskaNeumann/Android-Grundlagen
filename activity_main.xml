<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">



<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Währungsrechner"
    android:id="@+id/ueberschrift" />

<EditText
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:inputType="numberDecimal"
    android:layout_below="@+id/ueberschrift"
    android:hint="Zahl eingeben"
    android:id="@+id/eingabefeld"/>

<Button
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="EUR --> USD"
    android:id="@+id/button"
    android:layout_toRightOf="@+id/eingabefeld"
    android:layout_alignBottom="@+id/eingabefeld"
    android:onClick="umrechnenEuro"/>

<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/eingabefeld"
    android:id="@+id/ergebnisText"
    android:text="Ergebnis:"/>

<TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_below="@+id/eingabefeld"
    android:layout_toRightOf="@+id/ergebnisText"
    android:id="@+id/umgerechneterWert" />


</RelativeLayout>

